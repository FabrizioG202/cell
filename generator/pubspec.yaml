name: cell_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  analyzer:
  build:
  source_gen:
  cell:
    path: ../cell
  collection: ^1.18.0
  glob: ^2.1.2
  path: ^1.8.3
  dart_style:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  very_good_analysis:
